#pragma once
// ============================================================================
// WCS_httpServer.h — Qt Creator 自动生成的主窗口类
//
// ⚠️ 已废弃：实际使用 MainWindow 类，本类仅保留用于 Qt Designer 编译兼容
//    如需修改 UI，请编辑 MainWindow.ui 和 MainWindow.h/.cpp
// ============================================================================

#include <QtWidgets/QMainWindow>
#include "ui_WCS_httpServer.h"

class WCS_httpServer : public QMainWindow
{
    Q_OBJECT

public:
    WCS_httpServer(QWidget *parent = Q_NULLPTR);

private:
    Ui::WCS_httpServerClass ui;  // Qt Designer 自动生成的UI类（已废弃）
};
