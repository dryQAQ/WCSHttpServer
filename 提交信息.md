# 提交信息记录（长文本存档）

> 用途：存放每次提交的**完整提交信息**，避免提交时被截断/丢失，方便下次回顾与复用。
> 位置：仓库根目录（`WCS_httpServer\提交信息.md`），未被 `.gitignore` 忽略，随代码一起提交。
> 用法：每次提交前，把完整信息追加到下面「二、提交记录」章节（最新在最上面）；提交时直接从本文件复制标题行与正文。

## 一、书写规范（沿用本仓库既有风格）

- 标题：`[add]` / `[mod]` / `[bug]` + 一句话概括（可带分号串联多个模块）。
- 正文：编号 + 缩进列要点；每个要点写清 **改了什么 / 为什么 / 影响面 / 配置项 / 验证方式**。
- 涉及现场口径的改动，注明「默认值」与「客户端口」，便于日后核对。
- 提交前把本文件新增的章节一并 `git add`（否则内容只在本地）。

---

## 二、提交记录

### 2026-09-11 查询面板增强 + 同波次重扫重投 + 期望绑定数量=1 + 绑定面板已锁格计数

> 状态：**待提交**（工作区改动已暂存，提交时直接用下面这段作为提交信息）

```text
[add] 查询面板增强(按SKU列EPC/实际落格号、按格口7/007/22007归一) + 同波次重扫重投(重投按原格口) + 期望绑定数量=1 + 绑定面板已锁格计数

1. 分拣记录查询
   - 按SKU查询：新增「EPC编码」「实际落格号」列（一行=一个EPC↔其实际落格号），保留计划格口/格口类型/计划数量/已分拣数量/库位/容器号；
     无落格的计划格口出「待分拣」行；实际落格号不在计划内→橙色高亮并补「（计划外）」行；标签显示"计划N条明细(K个格口)/EPC落格明细M条(实际落在J个格口)"
   - 按格口号查询：输入 7 / 007 / 22007(WMS编码) 统一归一为内部3位key后再查询，UI表现与结果完全一致（标签/日志/列显示无差异）；
     库内混存(7/007)与历史WMS编码均可命中；全格口汇总按格口整数归组、统一3位显示（避免7/007重复成两行）
2. 同波次「重扫重投」（现场需求：已落格的件拿起重新上料，仍按原格口分配）
   - m_sentEpcs 语义由"本波次已发送过(永久去重)"改为"在途(已下发、未收到落格反馈)"：PLC落格反馈到达即出在途（覆盖成功/无匹配/无绑定/冲突/status2·3全部分支），之后重扫可重投原格口
   - 保护：冷却 rescanResendCooldownMs=1000、同一件每波次最多重投 rescanResendMaxTimes=3（首投不计入，超限拦截+异常表"重扫超限"）、PLC在途超时 plcInFlightTimeoutMs=30000 兜底
   - 计数口径：重扫落格不重复计件（同一实物件，箱内1件=账1件，H7/H8数量不虚高）；普通PLC重复反馈仍沿用需求8"计件不写明细"
   - 重扫落到非首落格口→WARN+异常表"重扫格口不一致"（账仍记首落，不改历史记录）
   - 修复：重扫推送时重置1s「RFID→PLC」发送超时窗口（原沿用旧计时起点会被误判"发送超时入异常口"，导致重投实际不可用）
   - 新增4项配置 + 启动配置摘要显示
3. 期望绑定数量默认 66→1（define.h / ConfigManager / 源配置补 expectedBindCount 项；release、mock 配置同步）
4. 容器绑定面板：新增黄色「已锁格: N」计数（紧跟"已绑定"，口径=PLC物理锁格格口数；橙色待重绑数量入tooltip），图例补「橙色=已解锁·待重绑」
5. 测试与文档：test/e2e_rescan_resend.py 扮演RFID推送/RFID查询/PLC三方驱动真实exe，实测19/20（首投/冷却拦截/重投原格口/在途去重/上限拦截+异常留痕/不重复计数；1项为脚本断言文案笔误已修）
   新增 doc/重扫重投口径.md（口径/配置/日志/边界/验证）

备注：本次提交随暂存区一并带入工作区中此前的未提交改动（PlcManager、MainWindow 局部）与既有构建产物/运行日志。
```

#### 拆分提交方案（若按功能分多次提交，用以下标题）

```text
[add] 分拣记录查询增强：按SKU显示每个EPC及其实际落格号；按格口号查询兼容 7/007/22007

[add] 同波次重扫重投：已落格件重新上料仍按原格口下发（在途去重+冷却1000ms+每波次上限3次+不重复计数）

[mod] 期望绑定数量默认值 66→1，源配置补充 expectedBindCount 项

[add] 容器绑定面板新增黄色"已锁格"计数
```

#### 本次建议暂存范围（排除构建产物与运行日志）

```bash
git add WCS_httpServer/HttpServer.h WCS_httpServer/HttpServer.cpp \
        WCS_httpServer/MainWindow.h WCS_httpServer/MainWindow.cpp \
        WCS_httpServer/define.h WCS_httpServer/ConfigManager.h WCS_httpServer/ConfigManager.cpp \
        WCS_httpServer/SortingDatabase.h WCS_httpServer/SortingDatabase.cpp \
        WCS_httpServer/WmsGridCode.h \
        WCS_httpServer/config/http_server.xml \
        release_WcsHttpServer/config/http_server.xml \
        mock_env/config_mock/http_server.xml \
        提交信息.md
```

#### 变更文件与关键落点（便于日后定位）

| 方向 | 文件 | 关键点 |
| --- | --- | --- |
| 查询增强 | `WCS_httpServer/define.h` | `SQL_QUERY_BY_GRID`（多写法兼容）、`SQL_QUERY_BY_SKU`、`SQL_QUERY_GRID_SUMMARY`（整数归组）、`SQL_SELECT_FIRST_GRID_BY_EPC` |
| 查询增强 | `WCS_httpServer/WmsGridCode.h` | `normalizeGridKey()`（7/007/22007 唯一归一实现，含前缀未配置兜底） |
| 查询增强 | `WCS_httpServer/SortingDatabase.h/.cpp` | `queryBySku()`、`queryByGrid()` 归一、`getFirstSortedGrid()` |
| 查询增强 | `WCS_httpServer/MainWindow.cpp` | 按SKU 展开 EPC↔实际落格号、按格口查询列/标签、全格口汇总 |
| 重扫重投 | `WCS_httpServer/HttpServer.h/.cpp` | `m_sentEpcs` 在途语义 + `isEpcInFlight/markEpcInFlight/clearEpcInFlight/clearAllEpcRuntimeState`、反馈入口出在途、重扫分支（冷却/上限/日志/异常）、重扫计时归零 |
| 重扫重投 | `WCS_httpServer/define.h`、`ConfigManager.h/.cpp`、3 份 `http_server.xml` | `rescanResendEnabled/CooldownMs/MaxTimes`、`plcInFlightTimeoutMs` |
| 绑定面板 | `WCS_httpServer/MainWindow.h/.cpp` | `m_lblLockedCount`（黄色已锁格计数）+ 图例补橙色态 |
| 期望绑定 | `WCS_httpServer/define.h`、`ConfigManager.*`、3 份 `http_server.xml` | `DEFAULT_EXPECTED_BIND_COUNT 66→1`，源配置补 `<expectedBindCount>` |
| 文档/测试 | `WCS_httpServer/doc/重扫重投口径.md`、`test/e2e_rescan_resend.py` | 口径说明；E2E 脚本（结果/日志/README 同目录，注意 doc 与 test 目前被 .gitignore 忽略） |

#### 遗留与待办（下次可继续）

- DB 级 EPC 防重查的是**从未写入的 `sort_txn` 表**（现场 0 行）→ "同波次号被 WMS 重下"时同一 EPC 会重复计数（现场库已有 10 组重复记录）；建议改查 `sorting_records`。
- 重扫重投的"H7 报文是否补齐重启前未满箱的旧记录"仍待定（上一波次恢复方案中的既有缺口）。

---

## 三、下次提交模板（复制后填）

```text
[add|mod|bug] 一句话标题（模块：改动要点）

1. <模块/功能>
   - 改了什么 / 为什么
   - 配置项与默认值（若有）
   - 影响面与兼容性
2. 测试与验证
   - 用例与结果（脚本路径、通过数）
3. 文档
   - 新增/更新了哪个文件

备注：<现场口径、待确认项、遗留问题>
```

---

## 四、历史提交信息（本文件建立前的近期提交，便于对照风格）

```text
15e4497 [add] 更新UI接收任务面板界面：重传数据队列可选择、失败队列、多波次任务下发接收队列
9b75f6c 补充忽略规则：崩溃转储、压缩包、备份文件
793e03c 迁移GitHub：提交当前全部改动
de19e65 [bug] 问题：计时统计面板与实时落格数量不一致
39e71ab [add] 界面优化、ui日志打印RFID反馈数据
```
